import 'package:flutter/material.dart';

class BackGroundLocation extends StatelessWidget {
const BackGroundLocation({Key? key}) : super(key: key);

@override
Widget build(BuildContext context) {
return Container();
}
}
